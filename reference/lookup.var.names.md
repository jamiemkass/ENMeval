# Look up predictor variable names from a fitted model

Internal function to get the predictor variable names used to fit a
model, given its algorithm.

## Usage

``` r
lookup.var.names(mod, algorithm)
```

## Arguments

- mod:

  A fitted model object.

- algorithm:

  character: algorithm name, as returned by \[lookup.algorithm()\].
