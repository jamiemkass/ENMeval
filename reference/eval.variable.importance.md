# eval.variable.importance (variable importance) generic for ENMevaluation object

eval.variable.importance (variable importance) generic for ENMevaluation
object

## Usage

``` r
eval.variable.importance(x)
```

## Arguments

- x:

  ENMevaluation object
