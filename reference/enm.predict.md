# enm.predict generic for ENMdetails object

enm.predict generic for ENMdetails object

## Usage

``` r
enm.predict(x)

enm.predict(x) <- value

# S4 method for class 'ENMdetails'
enm.predict(x)

# S4 method for class 'ENMdetails'
enm.predict(x) <- value
```

## Arguments

- x:

  ENMdetails object

- value:

  input value
