# eval.name generic for ENMdetails object

eval.name generic for ENMdetails object

## Usage

``` r
enm.name(x)

enm.name(x) <- value

# S4 method for class 'ENMdetails'
enm.name(x)

# S4 method for class 'ENMdetails'
enm.name(x) <- value
```

## Arguments

- x:

  ENMdetails object

- value:

  input value
